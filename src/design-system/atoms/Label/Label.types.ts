import * as LabelPrimitive from '@radix-ui/react-label';

/**
 * Label component props extending Radix UI Label primitive props.
 * Provides semantic labeling for form elements with design system styling.
 */
export type LabelProps = React.ComponentProps<typeof LabelPrimitive.Root>;
